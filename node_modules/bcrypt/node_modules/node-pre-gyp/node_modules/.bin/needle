#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_needle@2.6.0@needle/bin/needle" "$@"
  ret=$?
else 
  node  "$basedir/../../../_needle@2.6.0@needle/bin/needle" "$@"
  ret=$?
fi
exit $ret
